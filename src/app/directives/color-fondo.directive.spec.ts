import { ColorFondoDirective } from './color-fondo.directive';

describe('ColorFondoDirective', () => {
  it('should create an instance', () => {
    const directive = new ColorFondoDirective();
    expect(directive).toBeTruthy();
  });
});
